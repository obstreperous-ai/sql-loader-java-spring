# SQL Loader Configuration
# This is a minimal configuration file. Most settings should come from environment variables.

# Logging
logging.level.ai.obstreperous.sqlloader=INFO

# Disable Spring Boot banner for cleaner output
spring.main.banner-mode=off

# Exit immediately after running the command
spring.main.web-application-type=none
