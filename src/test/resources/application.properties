# Test configuration
# Disable banner
spring.main.banner-mode=off

# Disable web application
spring.main.web-application-type=none

# Enable the runner bean but prevent it from running (we'll test the method directly)
sqlloader.enabled=true

# Logging
logging.level.ai.obstreperous.sqlloader=INFO
